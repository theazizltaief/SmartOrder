<div id="panier">
  <h2>🛒 Votre commande</h2>
  <ul data-panier-target="liste"></ul>

<form data-panier-target="form" data-action="submit->panier#submit" action="/commandes" method="post">
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
    <%= hidden_field_tag "commande[table_id]", 1 %>
    <input type="hidden" name="commande[ligne_commandes_attributes]" data-panier-target="json" />
    <button type="submit">Envoyer la commande</button>
  </form>
</div>
